# 2026-08-17

## 重写 README.md
- 原 README 是另一个项目（Wan2.1/2.2 latent Resizer，作者 LBH-123-AI）的内容，与仓库实际代码完全不符，已重写。
- 仓库真实内容：`Comfyui_Minimax_h3_latent_Upscaler`，ComfyUI 自定义节点，对 Minimax H3 的 24 通道 VAE latent 做神经网络放大。
- 两个节点（`video/MinimaxH3` 分类）：
  - `MinimaxH3LatentUpscalerNode2D`（2D 主干 + Temporal 3D Conv，双线性插值）
  - `MinimaxH3LatentUpscalerNode3D`（纯 3D 卷积，三线性插值）
- 仅支持放大 scale 1.0–4.0（默认 2.0）；scale=1.0 直通；<1.0 报错。
- 模型从 `ComfyUI/models/latent_upscale_models/` 加载；自动检测结构；支持 .safetensors/.pth、FP8→FP16、`upscaler.` 前缀；推理强制 attn=False。
- 注意：README 中 git clone URL 用了 `YOUR_USERNAME` 占位符，用户需替换为真实仓库地址；模型权重不随仓库提供。
- 用户要求「可切换中英文」形式 → 改为双文件方案：`README.md`（英文默认，顶部语言横幅链接 README_zh.md）与 `README_zh.md`（中文，顶部链接回 README.md），互相跳转实现语言切换。

## 节点精度参数改造
- 将 `use_fp16` 布尔开关改为 `precision` 下拉框：`["fp32", "fp16", "bf16"]`，默认 `fp32`。
- 涉及两个节点文件：`minimax_h3_latent_upscaler_2d.py` 与 `minimax_h3_latent_upscaler_3d.py`。
- 改动点：`INPUT_TYPES`（precision 下拉）、`run()` 签名与 `compute_dtype` 映射、`load_model()` 签名/缓存键/`model.to(dtype)` 转换（bf16→torch.bfloat16）。
- 已 `py_compile` 语法校验通过；同步更新 README.md / README_zh.md 的节点参数表与特性说明。

## README 措辞优化（重点：加速生成、省时间不省显存）
- 用户核心目的：未来加速视频生成，流程为「H3 低清生成 → latent 放大 → 二次采样重绘」，节省的是**生成时间**而非显存。
- 重写中英文 README 的简介与「典型流程」：明确说明省时间不省显存（精修仍按目标分辨率跑，峰值显存与直接高清生成接近）。
- 修正原 README 中「drastically cuts VRAM usage and generation time / 显著降低显存占用与生成时间」的误导说法。
- precision 参数说明从「save VRAM」改为「use less memory and run faster / 显存占用更低、速度更快」（指模型 dtype 本身，非工作流）。

## README 措辞二次优化（补充技术细节）
- 用户补充核心价值：节省的是 **VAE 解码→像素放大→再编码** 的慢速往返时间（Minimax H3 自带约 5B 参数的笨重 VAE，编解码慢）；同时避免直接 latent 插值带来的**鬼影/重影**；作用对标 **LTX2.3** 的 latent 放大模型。
- 重写中英文 README 简介与「典型流程」：新增「跳过 decode→pixel-upscale→encode 往返」要点、鬼影/重影说明、LTX2.3 类比；工作流段落加入朴素做法对比 `[Latent]→[VAE Decode]→[Pixel Upscaler]→[VAE Encode]`。
- 保持「省时间不省显存」的核心口径不变。

## README 新增「训练数据」章节
- 用户补充模型训练规模：近 8 万对样本（视频约 70,000 对 + 2K 图像约 8,000 对）。
- 缩放倍数分布（约）：2× 占 40%（主力），1.5×/2.5×/3×/4× 各 10%，另有 1.0×–4.0× 任意小数位占 10% 用于增强泛化（避免过拟合到固定档位）。
- 中英文 README 各新增 `## 📊 Training Data / ## 📊 训练数据` 一节，置于「模型与架构」与「致谢」之间，含「按数据类型」「按缩放倍数」两张表及泛化说明。
